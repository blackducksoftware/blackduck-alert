ext.moduleName = 'com.blackduck.integration.alert.api-common-model'

dependencies {
    api 'com.blackduck.integration:integration-common'
    implementation platform(project(':alert-platform'))
    testImplementation project(':test-common')
}
