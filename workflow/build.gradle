ext.moduleName = 'com.synopsys.integration.alert.workflow'

dependencies {
    implementation platform(project(':alert-platform'))

    implementation project(':alert-common')
    implementation project(':alert-database')
    testImplementation project(':test-common')

    implementation 'org.springframework:spring-context'
    implementation 'org.springframework:spring-tx'
    implementation 'jakarta.annotation:jakarta.annotation-api'
    implementation 'jakarta.jms:jakarta.jms-api'
    implementation 'com.synopsys.integration:integration-rest'
    implementation 'com.synopsys.integration:integration-common'
    //FIXME We don't want this dependency, we want to abstract it out
    implementation 'org.springframework.data:spring-data-jpa'
    //TODO: Do not merge this into master yet, figure out why this isn't working when implementing :test-common
    testImplementation 'org.mockito:mockito-core'
}
